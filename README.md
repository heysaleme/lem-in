# Lem-in: Симуляция муравьиной фермы

Проект представляет собой эффективный менеджер логистики для цифровой муравьиной фермы. Задача программы — найти наиболее быстрый способ перевести  муравьев из комнаты `##start` в комнату `##end` по запутанной сети туннелей.

## 🐜 Суть проекта

Представь, что у тебя есть карта комнат, соединенных туннелями. В каждой комнате одновременно может находиться только **один** муравей (кроме старта и финиша), а по каждому туннелю за один ход может пройти только один муравей.

**Цель:** Вывести список ходов так, чтобы последний муравей оказался на финише за минимально возможное количество шагов.

## 🚀 Как запустить

Для работы программы необходим установленный [Go](https://go.dev/).

1. **Склонируйте репозиторий:**
```bash
git clone <ссылка-на-ваш-репозиторий>
cd lem-in
```


2. **Запустите программу с файлом карты:**
```bash
go run ./cmd/lem-in <exampleNN.txt>
```



### Пример вывода:

```text
L1-t L2-h L3-o
L1-E L2-A L3-o L4-t L5-h
...

```

Где `L1-t` означает: Муравей №1 перешел в комнату `t`.


## 🏗 Архитектура проекта

Проект построен по принципам **Clean Architecture** (чистая архитектура). Код разделен на независимые модули, что делает его профессиональным и легким в поддержке.

* `cmd/lem-in/` — **Точка входа**. Соединяет все части системы воедино.
* `internal/models/` — **Сердце проекта**. Описывает общие структуры данных (`Ant`, `Room`, `Path`, `Farm`).
* `internal/parser/` — **Считыватель**. Отвечает за чтение текстовых файлов, проверку ошибок в данных и создание первичной структуры фермы.
* `internal/graph/` — **Картограф**. Превращает текстовые данные в математический граф (список смежности).
* `internal/solver/` — **Мозг**. Самая сложная часть. Использует алгоритмы поиска путей и комбинаторную логику для выбора самого эффективного маршрута.
* `internal/simulation/` — **Двигатель**. Пошагово передвигает муравьев по выбранным путям, следя за тем, чтобы они не сталкивались.
* `internal/formatter/` — **Художник**. Выводит результат в консоль согласно требуемому формату.

## 🧠 Алгоритмическая логика

Программа не просто ищет кратчайший путь. Она решает задачу **оптимизации потока**:

1. **Поиск всех путей:** Используется рекурсивный поиск (DFS), чтобы найти все возможные способы дойти до финиша.
2. **Выбор комбинации:** Программа сравнивает разные наборы непересекающихся путей. Иногда выгодно запустить муравьев по трем длинным путям одновременно, чем по одному короткому.
3. **Математическое распределение:** Применяется формула для распределения муравьев, чтобы минимизировать общее время ожидания в очереди.

## 🛠 Технологии

* **Язык программирования:** Go (Golang)
* **Алгоритмы:** Поиск в глубину (DFS), комбинаторная оптимизация, расчет потоков.
* **Сложность:** O(V+E) для базового поиска, где V — количество комнат, E — количество туннелей.